import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#fff',
        paddingHorizontal: 16,
        marginBottom: 16,
        width: '100%',
        height: 48,
        borderRadius: 8,
        borderWidth: 1,
        borderColor: '#c3c5cb'
    }
})
